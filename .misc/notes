
install BFT-Smart: mvn install:install-file -Dfile=./core-0.1.4.jar -DgroupId=com.yahoo.ycsb -DartifactId=core -Dversion=0.1.4-Dpackaging=jar

<groupId>com.yahoo.ycsb</groupId>
			<artifactId>core</artifactId>
			<version>0.1.4</version>

how to enable signatures? where do i put the keys? how to use ecsa signatures instead of rsa?
how to enable ssl ??

ssltls

protocol_version

key_store_file